'use client'

import { HeroCanvas } from './hero'

export default function HeroSection() {
  return (
    <section id="heroSection" className="w-full h-screen">
      <HeroCanvas />
    </section>
  )
}
